// Phase 0 stub. Phase 1 will register `dev.cosmonaut.Installer1` on the
// system bus and expose the `Install`, `Cancel`, `Probe`, `ScanWifi`,
// `ConnectWifi` methods backed by `cosmonaut-engine`.

fn main() {
    eprintln!("cosmonaut-installer-daemon: Phase 0 stub");
    std::process::exit(0);
}
