// Phase 0 stub. Phase 1 will accept `--image`, `--disk`, optional
// `--luks-passphrase` / `--tpm2-luks` and call the daemon's
// `dev.cosmonaut.Installer1.Install` method.

fn main() {
    eprintln!("cosmonaut-installer-cli: Phase 0 stub");
    std::process::exit(0);
}
